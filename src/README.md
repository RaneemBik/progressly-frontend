# frontend/src

React application root.

| File | Purpose |
|---|---|
| `index.tsx` | Mounts React app into #root DOM element |
| `App.tsx` | Root component: providers (Store, Toast) + React Router routes |
| `src/` | All application source code (components, pages, services, types) |
