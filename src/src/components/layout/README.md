# frontend/src/src/components/layout

App-wide layout components rendered on every page.

| File | Purpose |
|---|---|
| `Navbar.tsx` | Fixed top bar with logo, user name, Home link, Logout |
| `Sidebar.tsx` | Collapsible left panel with Dashboard link and project list |
| `Footer.tsx` | Simple footer shown only on the landing page |
